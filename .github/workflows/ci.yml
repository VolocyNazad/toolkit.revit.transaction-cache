name: CI

on:
  push:
    branches:
    - main
  pull_request:

jobs:
  check-required-files:
    name: Check required files
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Verify required files exist
        run: |
          missing=()

          check() {
            local label="$1"; shift
            for f in "$@"; do
              if [ -e "$f" ]; then
                return 0
              fi
            done
            missing+=("$label")
          }

          check "LICENSE" LICENSE LICENSE.md LICENSE.txt
          check "README" README.md README README.txt
          check "AGENTS.md" AGENTS.md
          check "NuGet.config" NuGet.config nuget.config

          if [ ${#missing[@]} -ne 0 ]; then
            echo "::error::Missing required files: ${missing[*]}"
            exit 1
          fi

          echo "All required files are present."

  build:
    name: windows-latest
    needs: check-required-files
    runs-on: windows-latest
    strategy:
      matrix:
        configuration: [
          "Release_2021.1.9", "Release_2023.0.0", "Release_2025.0.0"
        ]

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
    - name: Check for version tag
      id: tag_check
      run: |
        $tag = git tag --points-at HEAD
        $isTagged = [bool]$tag
        Write-Host "Tag(s) at HEAD: $tag"
        echo "is_tagged=$($isTagged.ToString().ToLower())" >> $env:GITHUB_OUTPUT
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v4
    - name: Execute build and run tests
      run: |
        dotnet build --configuration "${{ matrix.configuration }}" /nologo

        # Revit.TransactionMemoryCache.RevitTests is deliberately excluded here: it's built above
        # (verifying it compiles against every Revit version), but it needs a real, licensed, running
        # Revit process to execute (Nice3point.TUnit.Revit's RevitApiTest) - not available on a
        # standard GitHub-hosted runner. Only the headless test projects run here.
        dotnet test --configuration "${{ matrix.configuration }}" --project tests/Revit.TransactionMemoryCache.Tests/Revit.TransactionMemoryCache.Tests.csproj
        dotnet test --configuration "${{ matrix.configuration }}" --project analyzers/Revit.TransactionMemoryCache.Analyzers.Tests/Revit.TransactionMemoryCache.Analyzers.Tests.csproj
    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: VolocyNazad.Revit.TransactionMemoryCache
        path: artifacts/*
    - name: Publish NuGet package
      if: github.event_name == 'push' && steps.tag_check.outputs.is_tagged == 'true'
      run: |
        foreach($file in (Get-ChildItem "artifacts/" -Recurse -Include *.nupkg)) {
            dotnet nuget push $file --api-key "${{ secrets.NUGET_API_KEY }}" --source https://api.nuget.org/v3/index.json --skip-duplicate
        }
